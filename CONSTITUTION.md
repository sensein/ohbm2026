# OHBM 2026 Pipeline Constitution

The canonical constitution has moved to
[`.specify/memory/constitution.md`](.specify/memory/constitution.md).

This file is a pointer kept at the repository root so existing markdown links
(in `README.md`, `docs/`, `experiments/`, and `memory/summary.md`) still
resolve. All amendments happen through `/speckit-constitution`, which writes
to the canonical location and propagates changes to dependent templates.

See [`.specify/memory/constitution.md`](.specify/memory/constitution.md) for
the current principles, operational constraints, delivery workflow, and
governance.
