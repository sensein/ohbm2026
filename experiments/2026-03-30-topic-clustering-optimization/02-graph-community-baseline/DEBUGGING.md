# Debugging

- A fresh semantic-analysis rerun was attempted on additional bundles, but the
  full modularity sweeps were too expensive to justify for this iteration.
- The experiment therefore reused the existing semantic-analysis baselines for
  the focused trio and documented the reason explicitly rather than silently
  dropping the graph baseline.
