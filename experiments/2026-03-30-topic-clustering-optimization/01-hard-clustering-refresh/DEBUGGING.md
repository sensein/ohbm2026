# Debugging

- Initialized plan before execution.
- User redirected the scope from a broader bundle scan to the focused trio:
  `voyage_stage2_published`, `openai_stage1`, and `minilm_stage1`.
- Earlier exploratory hard-clustering outputs for off-scope bundles were
  discarded from the tracked experiment notes.
